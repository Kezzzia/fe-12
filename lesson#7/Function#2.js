// 2. Напишите функцию perimeter, подсчитывающую периметр правильного многоугольника. 
// Первый параметр функции — side, длина стороны, второй параметр — count, количество 
// сторон в многоугольнике.

function perimeter(side, count) {
    return side * count;
}

perimeter(15, 65);